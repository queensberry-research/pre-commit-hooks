name: push
on:
  push:
    branches:
      - master
jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - name: Update CA certificates (tag)
        run: sudo update-ca-certificates
      - name: Tag latest commit
        uses: dycw/action-tag@latest
        with:
          token-uv: ${{secrets.ACTION_TOKEN}}
